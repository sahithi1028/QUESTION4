/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package Hello;

import java.util.ArrayList;
import java.util.List;

import javax.persistence.EntityManager;
import javax.persistence.EntityManagerFactory;
import javax.persistence.Persistence;

import com.Teacher;
import com.Student;
public class Library {
    public boolean someLibraryMethod() {
        return true;
    }
    public static void main(String[] args) {
		EntityManagerFactory emf = Persistence.createEntityManagerFactory("TeacherPU");
		EntityManager entitymanager = emf.createEntityManager();
		Teacher teach = new Teacher();
		teach.setTeacherId(1);
		teach.setTeacherName("Kiran");
		teach.setSubject("GVPCOE");
		
		List<Student> mylist = new ArrayList<Student>();
		mylist.add(new Student("Raju", "8th semester", teach));
		mylist.add(new Student("Rani", "8th semester", teach));
		mylist.add(new Student("Lakhay", "6th semester", teach));
		mylist.add(new Student("Minu", "2nd semester", teach));
		teach.setList(mylist);
		
		entitymanager.getTransaction().begin();
		entitymanager.persist(teach);
		entitymanager.flush();
		entitymanager.getTransaction().commit();
	}
}
